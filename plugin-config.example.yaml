plugins:
  enabled: true
  configs:
    cpa-reasoning-guard:
      enabled: true
      reasoning_guard: true
      # Applies only when a request already contains a reasoning object.
      repair_missing_effort: true
      default_reasoning_effort: high
